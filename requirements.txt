# ============================================================
# Smart Pick Pro — Strictly pinned runtime dependencies
# All versions use == to guarantee bit-for-bit reproducibility
# across dev / staging / production builds.
#
# To regenerate after any upgrade:
#   pip-compile requirements.in --generate-hashes -o requirements.txt
# ============================================================

# ── Web / UI ─────────────────────────────────────────────────
streamlit==1.45.1
streamlit-autorefresh==1.0.1
flask==3.1.0

# ── API / Async ───────────────────────────────────────────────
fastapi==0.110.3
uvicorn[standard]==0.29.0
aiohttp==3.9.5

# ── Data & ML ────────────────────────────────────────────────
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.4.2
xgboost==2.0.3
catboost==1.2.7
pyarrow==15.0.2
joblib==1.4.2

# ── NBA / Sports data ────────────────────────────────────────
nba_api==1.11.4

# ── HTTP clients ─────────────────────────────────────────────
requests==2.32.3
curl_cffi==0.7.4

# ── Payments ─────────────────────────────────────────────────
stripe==14.4.0

# ── Auth / Security ──────────────────────────────────────────
bcrypt==4.2.1
PyJWT==2.8.0

# ── Database ─────────────────────────────────────────────────
psycopg2-binary==2.9.10

# ── Parsing / Scraping ───────────────────────────────────────
pdfplumber==0.11.4
beautifulsoup4==4.12.3
lxml==5.3.0

# ── Utilities ────────────────────────────────────────────────
python-dotenv==1.0.1
thefuzz==0.22.1

# ── Telemetry & Observability ─────────────────────────────────
psutil~=5.9                  # System resource metrics (CPU, memory, disk) for admin dashboard
